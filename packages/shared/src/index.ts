/**
 * @folio/shared — platform-agnostic code shared between web, desktop, and mobile.
 *
 * This package will eventually contain:
 * - types (TreeNode, NoteFile, NoteFolder, FlatNode)
 * - supabase client & notesDb operations
 * - auth service logic
 * - sync engine (pending upserts/deletes queue)
 * - AI chat client (streamAiChat)
 * - pure utils (tree.ts, knowledgeBase.ts, noteMeta.ts, markdownText.ts)
 *
 * For now it re-exports nothing — migrate modules here incrementally.
 */

export {}
